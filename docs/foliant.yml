title: 'Cliar: Package for simple, extensible, and testable CLIs'
slug: cliar-docs

preprocessors:
  - includes:
      aliases:
        cliar: https://github.com/moigagoo/cliar.git

chapters:
  - index.md

backend_config:
  mkdocs:
    mkdocs.yml: !include mkdocs.yml
